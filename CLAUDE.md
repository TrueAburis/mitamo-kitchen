# みたもっちゃんねる 公式サイト

## このサイトについて

出汁ベースの和食を紹介する料理チャンネルの公式サイト。
運営者本人ではなく、制作・運営を請け負う立場で作っている。
見に来るのは (1) 動画を見て作り方を文字で読みたい日本の視聴者、
(2) 海外から日本の家庭料理を作ってみたい人（英語）。

このページの一番の仕事は、レシピを最後まで読ませて **実際に作ってもらうこと**。
次に、お仕事の依頼フォームに進んでもらうこと。

## このサイトの核（他と違う一点）

> **「読むレシピ」ではなく「作りながら見るレシピ」。分量が画面から消えない。**

料理サイトのほとんどは、手順までスクロールすると分量が画面外に消える。
そこを塞ぐことがこのサイトの存在理由。
迷ったら「この一点に貢献するか」で決める。貢献しない装飾は入れない。

## ファイル構成

1枚のHTMLで始めたが、全部が1ページに乗っているのが読みづらいため、2026年9月にページを分けた。
ビルドツールは使わない。HTMLを直接置くだけ。

```
index.html              トップ（見出し＋SNSボタン＋最新のレシピ＋プロフィール）
recipes.html            レシピ一覧
tori-mune-nasu-nanbanzuke.html  レシピ詳細（鶏むね肉と茄子の南蛮漬け）
tori-negi-meshi.html    レシピ詳細（鶏ネギ飯）
work.html               お仕事のご依頼（Google フォーム）

（about.html は 2026年9月にトップへ統合して削除した）
style.css               全ページ共通のCSS
script.js               全ページ共通のJS
images/                 写真
serve.ps1               開発用の簡易サーバー（サイト本体ではない）
.claude/skills/         このプロジェクト専用のスキル
```

**ヘッダーとフッターは各HTMLにコピーされている。**
片方だけ直すとズレるので、直したら全HTMLに同じ変更を入れること。

レシピを増やすときは `/add-recipe` スキルを使う（手順は `.claude/skills/add-recipe/SKILL.md`）。
中身としては `tori-negi-meshi.html` を複製して差し替える作業になる。
一覧は `recipes.html`。レシピを足したらこの一覧の先頭にも1行足す。
ナビの「レシピ」は常に `recipes.html` を指す。

表示を確認するときは、ターミナルで次を実行して http://localhost:8000/ を開く。

```
powershell -ExecutionPolicy Bypass -File serve.ps1
```

（このパソコンには Python も Node も入っていないため、PowerShell で立てている）

## 絶対に守るルール

- ページを増やすときは下の「ファイル構成」に従う。勝手にフォルダを掘らない
- 外部ライブラリ・ビルドツール・Webフォントを導入しない（ネットが無くても表示できる状態を保つ）
- 例外は1つだけ：お仕事のご依頼の Google フォーム（iframe）。これ以外の外部読み込みを増やさない
- 全テキストは日本語と英語を必ず対で用意する
- **材料名だけは日英を常に併記する**（切替で消さない）。海外の人が店頭のパッケージと照合できるようにするため
- 色は `:root` の CSS変数だけを使う。新しい色を直接書かない
- 動きをつけるのは1ページ1か所まで。現状の1か所は「分量バーが画面上端に貼り付く瞬間」
- 画像は 1:1 / 4:5 / 9:16 のどれが来ても崩れない枠に入れる（Instagram の投稿をそのまま流用する前提）
- 手順を角丸カードに切り分けない。左に1本の縦線を通し、待ち時間をその線で表す

## 使ってはいけない型（見慣れすぎているため）

- クリーム色（#F4F1EA あたり）の背景 + 明朝の見出し + テラコッタ色の差し色
- 真っ黒背景 + 蛍光グリーンか朱色の差し色ひとつ
- 見出しの上に、字間を広げた英字大文字の小さなラベル
- すべてを同じ角丸・同じ影のカードに切り分ける
- 意味のない 01 / 02 / 03 の番号（レシピの手順番号は意味があるので可）
- 中黒でつないだ meta 表記（A · B · C）
- ボタンやリンクの文字の末尾につける →
- 見出しの中で1単語だけ色を変える／太字にする
- 全セクションにスクロールで下からふわっと出るアニメーション
- ヒーローに縦長写真を角度をつけて重ねる

## 配色

写真が主役なので、UI 自体は色を持たない。色は写真から来る。

| 変数名 | 色 | 役割 | 状態 |
|---|---|---|---|
| `--ground` | `#FCFBF8` | 背景。ほぼ白の紙色 | 確定 |
| `--ink` | `#1E2320` | 本文。純黒を避けた、わずかに緑みの墨 | 確定 |
| `--ink-soft` | `#5C6360` | 補助文・英訳 | 確定 |
| `--rule` | `#DCD8CF` | 罫線。面ではなく線で区切る | 確定 |
| `--kombu` | `#24443A` | 昆布の深緑。面で大きく使う（フッター、分量バー） | **仮** |
| `--mark` | `#B4472E` | 印。1ページ2か所まで（現在地・時間表示のみ） | **仮** |

`--kombu` と `--mark` が **仮** なのは、Instagram の実写真がまだ無いため。
写真が入った時点で、写真から色を抜いて確定させる。
それまでは骨組みを無彩色（紙色・墨・罫線）だけで組む。

## 書体

Webフォントは使わない。OS 標準の書体だけで組む。

| 用途 | 書体 | 理由 |
|---|---|---|
| 和文すべて（本文も見出しも） | ゴシック。差はウェイトと大きさだけ | 見出しだけ明朝で雰囲気を出す作りをしない |
| **数字・分量・時間** | **等幅** | `150ml` `大さじ1` `弱火 8分` が縦に揃う。このサイトの中心 |
| 英文 | 和文と同系のサンセリフ | 日英でトーンを揃える |

## 作業のときのお願い

- 変更したら、触ったファイル名と行数を報告する
- 既存のレシピ表示が壊れていないか、自分で確認してから報告する
- 1セクション作り終えたら、コミットを促す
- 出したものを一度は自分で批評する。褒めなくていい

## まだ決まっていないこと（勝手に埋めない）

- チャンネル名の正式表記・ロゴ
- YouTube / Instagram の実 URL
- 本番用の連絡先メールアドレス。テスト用の個人アドレスは、公開リポジトリに載せないため work.html から外してある（雛形はコメントで残してある）
- 「このチャンネルについて」の本文（いまは仮の文章）と、はじめた年・更新頻度・動画の実データ
- レシピの実データ（現状ゼロ件。旧サイトには「鶏ネギ飯」1件のみあった）

これらはコード中で `TODO:` と明記し、それらしい嘘で埋めないこと。

## 仮の文章の扱い

まだ本番の文章が来ていない箇所は `class="draft"` を付けている。
画面上に赤い縦線と「仮の文章」の目印が出る。
本番の文章に差し替えたら `draft` クラスだけ外す。HTMLの中では
`▼▼▼ ここから仮の文章 ▼▼▼` のコメントで挟んであるので、そこを探せばよい。

実データが無い数値は `TODO` と書いておく。それらしい嘘で埋めない。

## SNS

トップの SNS ボタンから飛ぶ先。ここ以外に書かないこと。

| | URL |
|---|---|
| Instagram | https://www.instagram.com/mitamo.kitchen/ |
| TikTok | https://www.tiktok.com/@mitamokitchen |
| YouTube | https://www.youtube.com/channel/UCY5Py2DIsgdurOYtN0ayV3A |

アイコンは公式ロゴではなく、こちらで描いた簡略版のSVG（`index.html` に直接書いてある）。
正式なブランドロゴに差し替えたくなったら、各社の利用規約を確認してから入れ替える。

トップの構成は BayashiTV の公式サイトを参考にしている。
ただし、あちらが使っている「字間を広げた英字大文字の小さなラベル」は
使ってはいけない型に入れているので真似しない。

## トップに置くレシピについて

「今週の一品」ではなく「最新のレシピ」と呼ぶ。
更新が週1で続く保証がないため、名前が嘘になるのを避けている。

トップに載せるのは**主な材料だけ**（鶏ネギ飯ならネギダレの4品は載せない）。
レシピページと丸ごと同じ内容にすると、2回とも読み流されるため。
