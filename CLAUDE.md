# みたもっちゃんねる 公式サイト

## このサイトについて

出汁ベースの和食を紹介する料理チャンネルの公式サイト。
運営者本人ではなく、制作・運営を請け負う立場で作っている。
見に来るのは (1) 動画を見て作り方を文字で読みたい日本の視聴者、
(2) 海外から日本の家庭料理を作ってみたい人（英語）。

このページの一番の仕事は、レシピを最後まで読ませて **実際に作ってもらうこと**。
次に、お仕事の依頼フォームに進んでもらうこと。

## このサイトの核（他と違う一点）

> **「読むレシピ」ではなく「作りながら見るレシピ」。分量が画面から消えない。**

料理サイトのほとんどは、手順までスクロールすると分量が画面外に消える。
そこを塞ぐことがこのサイトの存在理由。
迷ったら「この一点に貢献するか」で決める。貢献しない装飾は入れない。

## ファイル構成

1枚のHTMLで始めたが、全部が1ページに乗っているのが読みづらいため、2026年9月にページを分けた。
ビルドツールは使わない。HTMLを直接置くだけ。

```
index.html              トップ（見出し＋SNSボタン＋最新のレシピ＋プロフィール）
recipes.html            レシピ一覧（検索・タグ絞り込み・並び替え）
tori-mune-nasu-nanbanzuke.html  レシピ詳細（鶏むね肉と茄子の南蛮漬け）
tori-negi-meshi.html    レシピ詳細（鶏ネギ飯）
work.html               お仕事のご依頼（Google フォーム）
style.css               全ページ共通のCSS
script.js               全ページ共通のJS
recipes.js              レシピのメタデータ（一覧・検索・人気順の元データ）
images/                 写真
serve.ps1               開発用の簡易サーバー（サイト本体ではない）
.claude/skills/         このプロジェクト専用のスキル
docs/ledger.html        制作台帳の元ファイル（サイト本体ではない）
```

（about.html は 2026年9月にトップへ統合して削除した）

**ヘッダーとフッターは各HTMLにコピーされている。**
片方だけ直すとズレるので、直したら全HTMLに同じ変更を入れること。

レシピを増やすときは `/add-recipe` スキルを使う（手順は `.claude/skills/add-recipe/SKILL.md`）。
中身としては `tori-negi-meshi.html` を複製して差し替える作業になる。
一覧は `recipes.html`。レシピを足したらこの一覧の先頭にも1行足す。
ナビの「レシピ」は常に `recipes.html` を指す。

表示を確認するときは、ターミナルで次を実行して http://localhost:8000/ を開く。

```
powershell -ExecutionPolicy Bypass -File serve.ps1
```

（このパソコンには Python も Node も入っていないため、PowerShell で立てている）

## 絶対に守るルール

- ページを増やすときは下の「ファイル構成」に従う。勝手にフォルダを掘らない
- 外部ライブラリ・ビルドツール・Webフォントを導入しない（ネットが無くても表示できる状態を保つ）
- 外部読み込みは次の2つだけ。これ以外を増やさない
  1. お仕事のご依頼の Google フォーム（iframe）
  2. レシピページの Instagram 動画の埋め込み（iframe）
- その iframe の高さは実測値。フォームの質問を増減したら、iframe の内側にスクロールバーが出ないか確認して高さを直す（内側に別のスクロールができると、スマホで操作しづらくなる）
- 全テキストは日本語と英語を必ず対で用意する
- **材料名だけは日英を常に併記する**（切替で消さない）。海外の人が店頭のパッケージと照合できるようにするため
- 常に表示する英語（材料名や英語表記の名前）の span には `lang="en"` を付ける。日本語表示のまま英語が混ざるので、付けないと読み上げが日本語の発音になる
- 色は `:root` の CSS変数だけを使う。新しい色を直接書かない
- 動きをつけるのは1ページ1か所まで。レシピガチャで引いた結果が1枚ずつ出てくる部分が、このサイトで唯一の動き
- 画像は 1:1 / 4:5 / 9:16 のどれが来ても崩れない枠に入れる（Instagram の投稿をそのまま流用する前提）
- 手順を角丸カードに切り分けない。左に1本の縦線を通し、待ち時間をその線で表す

## 使ってはいけない型（見慣れすぎているため）

- クリーム色（#F4F1EA あたり）の背景 + 明朝の見出し + テラコッタ色の差し色
- 真っ黒背景 + 蛍光グリーンか朱色の差し色ひとつ
- 見出しの上に、字間を広げた英字大文字の小さなラベル
- すべてを同じ角丸・同じ影のカードに切り分ける（レシピ一覧はカードを並べるが、角丸と影を使わず、写真の枠線と余白だけで組んでいる）
- 意味のない 01 / 02 / 03 の番号（レシピの手順番号は意味があるので可）
- 中黒でつないだ meta 表記（A · B · C）
- ボタンやリンクの文字の末尾につける →
- 見出しの中で1単語だけ色を変える／太字にする
- 全セクションにスクロールで下からふわっと出るアニメーション
- ヒーローに縦長写真を角度をつけて重ねる

## 配色

写真が主役なので、UI 自体は色を持たない。色は写真から来る。

| 変数名 | 色 | 役割 | 状態 |
|---|---|---|---|
| `--ground` | `#FCFBF8` | 背景。ほぼ白の紙色 | 確定 |
| `--ink` | `#1E2320` | 本文。純黒を避けた、わずかに緑みの墨 | 確定 |
| `--ink-soft` | `#5C6360` | 補助文・英訳 | 確定 |
| `--rule` | `#DCD8CF` | 罫線。面ではなく線で区切る | 確定 |
| `--kombu` | `#24443A` | 昆布の深緑。面で大きく使う（フッター、分量バー） | **仮** |
| `--mark` | `#B4472E` | 印。1ページ2か所まで（現在地・時間表示のみ） | **仮** |

`--kombu` と `--mark` が **仮** なのは、Instagram の実写真がまだ無いため。
写真が入った時点で、写真から色を抜いて確定させる。
それまでは骨組みを無彩色（紙色・墨・罫線）だけで組む。

## 書体

Webフォントは使わない。OS 標準の書体だけで組む。

| 用途 | 書体 | 理由 |
|---|---|---|
| 和文すべて（本文も見出しも） | ゴシック。差はウェイトと大きさだけ | 見出しだけ明朝で雰囲気を出す作りをしない |
| **数字・分量・時間** | **等幅** | `150ml` `大さじ1` `弱火 8分` が縦に揃う。このサイトの中心 |
| 英文 | 和文と同系のサンセリフ | 日英でトーンを揃える |

## 作業のときのお願い

- 変更したら、触ったファイル名と行数を報告する
- 既存のレシピ表示が壊れていないか、自分で確認してから報告する
- 1セクション作り終えたら、コミットを促す
- 出したものを一度は自分で批評する。褒めなくていい

## まだ決まっていないこと（勝手に埋めない）

- チャンネル名の正式表記・ロゴ
- 本番用の連絡先メールアドレス。テスト用の個人アドレスは、公開リポジトリに載せないため work.html から外してある（雛形はコメントで残してある）
- 「このチャンネルについて」の本文（いまは仮の文章）と、はじめた年・更新頻度・拠点の実データ
- 鶏むね肉と茄子の南蛮漬けの**手順**（Instagram のキャプションに記載が無かった）、何人前、時間、鶏むね肉の分量
- すべてのレシピの写真（images/ が空。Instagram の投稿から持ってくる予定）

これらはコード中で `TODO:` と明記し、それらしい嘘で埋めないこと。

## 仮の文章の扱い

まだ本番の文章が来ていない箇所は `class="draft"` を付けている。
画面上に赤い縦線と「仮の文章」の目印が出る。
本番の文章に差し替えたら `draft` クラスだけ外す。HTMLの中では
`▼▼▼ ここから仮の文章 ▼▼▼` のコメントで挟んであるので、そこを探せばよい。

実データが無い数値は `TODO` と書いておく。それらしい嘘で埋めない。

## SNS

トップの SNS ボタンから飛ぶ先。ここ以外に書かないこと。

| | URL |
|---|---|
| Instagram | https://www.instagram.com/mitamo.kitchen/ |
| TikTok | https://www.tiktok.com/@mitamokitchen |
| YouTube | https://www.youtube.com/channel/UCY5Py2DIsgdurOYtN0ayV3A |

アイコンは公式ロゴではなく、こちらで描いた簡略版のSVG（`index.html` に直接書いてある）。
正式なブランドロゴに差し替えたくなったら、各社の利用規約を確認してから入れ替える。

トップの構成は BayashiTV の公式サイトを参考にしている。
ただし、あちらが使っている「字間を広げた英字大文字の小さなラベル」は
使ってはいけない型に入れているので真似しない。

## トップに置くレシピについて

「今週の一品」ではなく「最新のレシピ」と呼ぶ。
更新が週1で続く保証がないため、名前が嘘になるのを避けている。

トップに載せるのは**主な材料だけ**（鶏ネギ飯ならネギダレの4品は載せない）。
レシピページと丸ごと同じ内容にすると、2回とも読み流されるため。

## レシピのデータ（recipes.js）

一覧・検索・タグ・人気順は、すべて `recipes.js` の `window.RECIPES` を読んで作っている。
**レシピを足したら、必ずここにも1件足す。** ページを作っただけでは一覧に出てこない。

各レシピが持つもの。

| キー | 意味 |
|---|---|
| `slug` | ファイル名と `<article id>` に一致させる |
| `ja` / `en` | タイトルと一行説明 |
| `tags` | `window.TAGS` のキーの配列。表示名は TAGS 側で一元管理する |
| `image` | `images/<slug>.jpg` |
| `posted` | `YYYY-MM-DD`。新しい順の並びに使う |
| `instagram` | 投稿のURL。あればレシピページに動画が埋め込まれる |
| `likes` / `comments` / `views` | Instagram から取り込んだ数値 |
| `ready` | 手順が入っていれば true。false なら一覧に「手順は準備中」と出る |

**`likes` などが未取得のときは `null` にする。`0` と書かない。**
`0` は「いいねが0件」という別の意味になり、嘘になる。
`null` は画面に「—」と出て、注記で未取得だと分かるようにしてある。

## Instagram の自動取り込み（これから作る）

方針だけ先に決めてある。実装はまだ入っていない。

- GitHub Actions が定期的に Instagram Graph API を叩き、`recipes.js` の数値を書き換えてコミットする
- Facebook ページは不要。Instagram Login を使う方式で、Instagram アカウントだけで完結する
- 長期アクセストークンの寿命は60日。更新も同じ仕組みの中で自動化する
- トークンは GitHub の Secrets に置く。**リポジトリのファイルには絶対に書かない**

**新規投稿を自動でレシピページにはしない。**
キャプションに手順が無いことがあるため（南蛮漬けが実例）。
自動で増えるのは新着の情報までとし、分量表と手順は人が組んでレシピページに昇格させる。

## PR（タイアップ）投稿の扱い

**自動投稿の対象から外す。** `tools/parse-caption.js` が `#PR` `#ad` などから判定する。

理由は3つある。

1. タイアップ契約が、Instagram の投稿だけを対象にしているのか、サイトへの転載まで含むのかは案件ごとに違う
2. 日本では2023年10月からステマ規制があり、広告なら広告と分かる表示が要る
3. 商品名とブランド名がそのまま載るため、後から消しにくい

載せると決めた回だけ、手で追加する。そのときは PR である旨を画面に明記すること。

## レシピガチャ（gacha.html）

「今日なにを作るか決まらない」を解くためのページ。単なる遊びではなく、
このサイトで一番よく起きる利用場面に当てている。

- **重複しないように引く。** 同じ料理が並んでも献立にならないため
- そのぶん、引ける数はレシピの数が上限になる。足りないときは
  「10連を引きましたが、いま引ける対象は2件です」と正直に出す。黙って2件だけ出さない
- タグで絞ってから引ける（「鶏肉で、何か1品」という引き方ができる）

## 制作台帳（みたもさんと共有しているページ）

進捗・要件・確認待ちの一覧を1枚にまとめたページ。**みたもさんに共有してある。**

- 元ファイル：`docs/ledger.html`
- 公開URL：**https://claude.ai/code/artifact/58e12afd-b45c-4465-b217-4a7c98f1c160**

**この URL は変えない。** みたもさんはこのリンクをブックマークしている前提。
更新するときは `docs/ledger.html` を書き換えて、**同じ URL を指定して再公開**する。
URL を指定せずに公開すると別のページが増えてしまい、
みたもさんは古いほうを見続けることになる。

更新の手順は `/ledger` スキルにまとめてある。

### 台帳に書くこと・書かないこと

- 書く：いまの状態、これからやること、みたもさんに聞くこと、できないと分かったこと
- 書かない：誰がいつ何を直したかの詳細（Git の履歴が持っている。二重管理すると必ずずれる）

「更新履歴」の節には、みたもさんが**前に見たときから何が変わったか**だけを、
1行で分かる粒度で足す。コミットメッセージをそのまま貼らない。

## タグの設計

食材・料理の形・場面・時間の4軸。`recipes.js` の `TAGS` に軸ごと定義してある。
材料の分類だけにしないのは、探す人が「弁当に入れたい」「15分しかない」で
探すため。材料の軸だけでは、その問いに答えられない。

**同じタグが `TAG_MIN`（3）件そろうまで、そのタグは画面に出さない。**
押しても1件しか出ないタグは、押した人をがっかりさせるだけなので伏せておく。
データとしては持っているので、件数がそろえば自動で表に出る。
表に出ているタグには件数を添えて、押す前に期待値が分かるようにしてある。

## 英語が無い回の扱い

キャプションに英語が無い回がある（実際に小籠包の回がそうだった）。そのときは：

- **日本語だけで公開する。** 公開を止めると、更新が止まったことに誰も気づかない
- **材料表だけは `tools/ingredients-ja-en.js` の辞書で英語にする**
- 本文は日本語のみである旨を画面に出す。後から英語が来たら差し替える

**機械翻訳は使わない。** 海外の人がこのサイトを見る一番の目的が
「店頭でパッケージと照合すること」なので、薄口醤油と濃口醤油を
訳し分けられない翻訳は、その目的に対して害になる。
辞書に無い材料は、訳さずに日本語のまま残す。間違った英語より、
日本語のままのほうが辞書やアプリで調べられる。

## 言語ごとに別URLにする（実装済み）

英語での検索流入を本気で取りにいくため、`/en/` に分ける。2026年9月6日決定。

```
/index.html          日本語
/en/index.html       英語
```

- 各ページは片方の言語だけを含む。いまの `.t-ja` / `.t-en` の対は無くなる
- 言語切替ボタンは、同じページの別言語版へのリンクになる
- `hreflang` で相互に指す。**絶対URLでないと効かない**ので独自ドメインが要る
- **材料名の日英併記は残す。** 英語ページでも日本語名を併記する。
  海外の人が店頭でパッケージと照合するためであって、翻訳のためではないので
- **英語の本文が無い回は、英語ページを作らない。**
  中身が日本語のままのページを英語として出すと、検索評価を下げるだけになる。
  つまり英語の流入は、キャプションに英語が書かれているかに依存する

ページが2倍になるため、**HTMLは手で書かない。`node tools/build.js` が組み立てる。**

## JSON-LD（Google のレシピ検索対策）

**手順・分量・時間がそろっているレシピにだけ書く。**
手順が無いレシピに中途半端な記述を入れても検索結果には出ないし、
それらしい嘘の手順で埋めるのは論外なので、書かないほうがよい。

写真が入るまでは、検索結果に写真つきでは出ない。`image` は
写真を置いた時点で絶対URLで足す（相対パスでは効かない）。

## 開発用の道具（Node）

**サイト本体はビルド不要のまま。** Node は `tools/` の中だけで使う道具で、
`serve.ps1` と同じ位置づけ。サイトのHTMLは Node が無くてもそのまま動く。
`package.json` に外部パッケージは入れない（依存ゼロを保つ）。

```
npm test        キャプション解析と材料辞書の検証（tools/test.js）
```

`tools/test.js` は実際の投稿3本に対して、取れるはずのものが取れているかを確かめる。
キャプションの書き方は回によって揺れるので、解析を触ったら必ず走らせること。

## キャプションからレシピページを作る

```
node tools/generate.js tools/fixtures/somen.txt <slug>
```

`<slug>.html` を書き出し、`recipes.js` に貼る1件分を表示する。

- ヘッダーとフッターは `index.html` から読み取って使う。
  テンプレートとして別に持つと、片方だけ直したときにズレるため
- **タグは `recipes.js` の TAGS にあるものしか付けない。**
  本文から新しいタグ名を作らせると「お手軽」「手軽」「かんたん」が
  別タグとして増殖して、タグ検索そのものが機能しなくなる
- 香味野菜（ネギ・生姜・にんにく・大葉・みょうが）は「野菜」として数えない。
  ネギが入っているだけの料理を野菜料理として出すと、押した人の期待から外れる
- **PR案件（#PR）は生成を中止する。** 終了コード 2 を返す
- 手順が取れなければ `ready:false`。画面に「手順は準備中」と出る。
  それらしい手順を書き足すことは絶対にしない

## ページの組み立て（重要）

**HTMLファイルを手で直さないこと。** 上書きされる。

```
node tools/build.js        全ページを日本語版と英語版で書き出す（14ページ）
npm test                   キャプション解析と材料辞書の検証
```

| 直したいもの | 直す場所 |
|---|---|
| ヘッダー・フッター・メニュー | `tools/build.js` の `T` |
| トップ・一覧・ガチャ・依頼ページの文章 | `tools/build-pages.js` の `C` |
| レシピの本文（材料・手順・コツ） | `content/<slug>.js` |
| レシピの一覧情報（タグ・いいね数など） | `recipes.js` |
| 見た目 | `style.css` |
| 画面の動き | `script.js` |

`style.css` と `script.js` は生成の対象外。直接編集してよい。

### 言語

- 日本語は `/`、英語は `/en/` に出る
- 言語切替はリンク。同じページの別言語版へ移動する（JSでの表示切替はもうしない）
- リンクは相対パスなので、英語ページのリンクは英語ページ同士でつながる
- **英語の本文が無いレシピは、英語ページを作らない。**
  中身が日本語のままのページを「英語です」と出すと評価を下げるため
- **材料名は英語ページでも日本語を併記する。** 主と従が入れ替わるだけ。
  店頭でパッケージと照合するためであって、翻訳のためではない

### ドメイン

`tools/build.js` の `SITE_URL` が唯一の出どころ。
hreflang と og:url は絶対URLでないと効かないので、
独自ドメインが決まったらここだけ直して組み立て直す。
