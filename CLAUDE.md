# みたもっちゃんねる 公式サイト

## このサイトについて

出汁ベースの和食を紹介する料理チャンネルの公式サイト。
運営者本人ではなく、制作・運営を請け負う立場で作っている。
見に来るのは (1) 動画を見て作り方を文字で読みたい日本の視聴者、
(2) 海外から日本の家庭料理を作ってみたい人（英語）。

このページの一番の仕事は、レシピを最後まで読ませて **実際に作ってもらうこと**。
次に、お仕事の依頼フォームに進んでもらうこと。

## このサイトの核（他と違う一点）

> **「読むレシピ」ではなく「作りながら見るレシピ」。分量が画面から消えない。**

料理サイトのほとんどは、手順までスクロールすると分量が画面外に消える。
そこを塞ぐことがこのサイトの存在理由。
迷ったら「この一点に貢献するか」で決める。貢献しない装飾は入れない。

## ファイル構成

いちばん大事な線は、**人が書くもの**と**機械が作るもの**の境。
書いたものは `dist/` に出る。**`dist/` の中は手で直さない。次の組み立てで消える。**

```
人が書く（git で追う）
  data/recipes.ts       レシピの一覧情報
  data/collections.ts   献立
  data/phrasebook.ts    キャプションの日英対訳
  data/captions/        みたもさんからもらったキャプション本文
  data/instagram.json   いいね数など ←ここだけ機械が書き換える
  content/<slug>.js     レシピ本文＝材料・手順・コツ
  assets/style.css      全ページ共通のCSS
  assets/script.js      全ページ共通のJS
  assets/images/        写真
  tools/                組み立てと検証の道具（TypeScript）
  infra/                公開用インフラの定義（AWS CDK）

機械が作る（git で追わない）
  dist/                 公開するものが全部ここに入る
  dist/index.html …     日本語のページ
  dist/en/…             英語のページ
  dist/recipes.js       一覧・検索・ガチャが読むデータ
  dist/style.css        assets/ から写したもの

そのほか
  serve.ps1             確認用サーバー（dist/ を配る）
  docs/ledger.html      制作台帳。みたもさんと共有しているページ
  docs/phrasebook.html  ←【生成物】日英対訳表。data/phrasebook.ts から作る
  .claude/skills/       このプロジェクト専用のスキル
```

**`dist/` を git に入れていない理由。** ソースから何度でも同じものが作れるため。
入れると、文章を1文字直すたびに46ファイルの差分が出て、
本当の変更が履歴の中で埋もれる。公開は GitHub Actions がその場で組み立てて送る。

**置き場所を決めているのは `tools/paths.ts` だけ。**
書き出し先を動かすときは、そこだけ直せばよいようにしてある。

### 直したいものと、直す場所

| 直したいもの | 直す場所 |
|---|---|
| ヘッダー・フッター・メニュー | `tools/build.ts` の `T` |
| 各ページの文章 | `tools/build-pages.ts` の `C` |
| レシピの材料・手順・コツ | `content/<slug>.js` |
| レシピのタグ・投稿URL | `data/recipes.ts` |
| 献立の組み合わせ | `data/collections.ts` |
| キャプションの英文の直し | `data/phrasebook.ts` の POSTS |
| 材料名・言い回しの定訳 | `data/phrasebook.ts` の TERMS と `tools/ingredients-ja-en.ts` |
| 見た目 | `assets/style.css` |
| 画面の動き | `assets/script.js` |
| ファイルの置き場所そのもの | `tools/paths.ts` |

### よく使うコマンド

```
npm run build    dist/ に全ページを日本語版と英語版で書き出す（46ページ）
npm test         キャプション解析・材料辞書・書き出した英文の検証（41項目）
npm run check    型検査
npm run build:phrasebook    日英対訳表（docs/phrasebook.html）を書き出す
powershell -ExecutionPolicy Bypass -File serve.ps1    確認用サーバー（先に build が要る）
```

レシピを増やすときは `/add-recipe` スキルを使う。

## 守るルール

2026年9月に見直した。**禁止ではなく、判断の材料で縛る**形にしている。
「素のHTMLだけ」「ライブラリ禁止」といった縛りは外した。
道具を禁じると判断が止まるが、予算があれば判断できるため。

### 1. 道具は目的で選ぶ。入れたら理由を書く

外部のライブラリも、TypeScript のような言語も使ってよい。
ただし**入れたものは、なぜ入れたかをコードのコメントかこのファイルに残す**。
理由を書けないものは、たいてい要らない。

### 2. 速さの予算

これを守れる限り、何を使ってもよい。破るなら先に相談する。

| | 上限 |
|---|---|
| 最初の表示に必要な JavaScript | **0KB**（ページは静的HTMLのまま） |
| 1ページの転送量（写真を除く） | **150KB** |
| 外部から読み込むもの | **押されるまで読み込まない** |

Instagram の動画と Google フォームが唯一の外部読み込み。
どちらも押されるまで読み込まない作りにしてある。

### 3. 動きは、状態が変わったことを伝えるときだけ

装飾で足さない。いま入っているのは3つだけで、すべて意味がある。

- ガチャで引いた結果が1枚ずつ出る（引いたことへの反応）
- 分量バーが貼り付いたときに影が出る（固定されたという合図）
- 触ったものの色が変わる（押せると伝える）

「全セクションがスクロールでふわっと出る」は入れない。中身が無いのに動くと安っぽくなる。

### 4. 英語まわり

- 全テキストを日本語と英語で用意する
- **材料名は、英語ページでも日本語を併記する。** 主と従が入れ替わるだけ。
  店頭でパッケージと照合するためであって、翻訳のためではない
- **英語の本文が無いレシピは、英語ページを作らない。**
  中身が日本語のままのページを「英語です」と出すと評価を下げる
- 辞書に無い材料は訳さず日本語のまま残す。間違った英語より、日本語のほうが調べられる

### 5. 推測で埋めない

- 情報が無い箇所は `TODO` と書く
- 数値が未取得のときは `null`。`0` と書かない（「0件」という別の意味になる）
- **手順が無いレシピに、それらしい手順を書き足さない**

### 6. PR（タイアップ）投稿は自動投稿の対象外

契約範囲が案件ごとに違う。載せる回だけ手で追加し、PRである旨を明記する。

### 7. 見た目の約束

- 色は `:root` の変数だけを使う。新しい色を直接書かない
- 画像は 1:1 / 4:5 / 9:16 のどれが来ても崩れない枠に入れる
- 手順を角丸カードに切り分けない。左に1本の縦線を通し、待ち時間をその線で表す
- 見出しだけ明朝、本文はOS標準のゴシック、数字と分量は等幅

## 使ってはいけない型（見慣れすぎているため）

- クリーム色（#F4F1EA あたり）の背景 + 明朝の見出し + テラコッタ色の差し色
- 真っ黒背景 + 蛍光グリーンか朱色の差し色ひとつ
- 見出しの上に、字間を広げた英字大文字の小さなラベル
- すべてを同じ角丸・同じ影のカードに切り分ける（レシピ一覧はカードを並べるが、角丸と影を使わず、写真の枠線と余白だけで組んでいる）
- 意味のない 01 / 02 / 03 の番号（レシピの手順番号は意味があるので可）
- 中黒でつないだ meta 表記（A · B · C）
- ボタンやリンクの文字の末尾につける →
- 見出しの中で1単語だけ色を変える／太字にする
- 全セクションにスクロールで下からふわっと出るアニメーション
- ヒーローに縦長写真を角度をつけて重ねる

## 配色

写真が主役なので、UI 自体は強い色を持たない。色は写真から来る。

| 変数名 | 色 | 役割 |
|---|---|---|
| `--ground` | `#FCFBF8` | 背景。ほぼ白の紙色 |
| `--ink` | `#1F2328` | 本文。純黒を避けた、わずかに青みの墨 |
| `--ink-soft` | `#5E6670` | 補助文 |
| `--rule` | `#DCD6CC` | 罫線。面ではなく線で区切る |
| `--indigo` | `#33465A` | 藍鼠。面で大きく使う（フッター、分量バー） |
| `--mark` | `#9E5A63` | 蘇芳。印として少量（時間の目盛り・注意） |

**深緑をやめて藍にした理由。** 料理写真は暖色（醤油の褐色、揚げ色、卵の黄）なので、
同じ暖色系を面に使うと互いを弱め合う。寒色の藍にすると写真が前に出る。
写真が主役という方針に、いちばん忠実な選択。

朱 `#B4472E` から蘇芳に変えたのは、クリーム背景＋暖色の差し色という
禁止した型に片足が入っていたため。赤みは保ったまま、そこから外した。

写真が入ったら、濃さだけ写真に合わせて調整する。色の出どころは `:root` だけ。

## 書体

**見出しだけ Webフォント（Zen Old Mincho）を読み込む。本文はOS標準のまま。**

OS標準だけで組むと、閲覧者の環境ごとに顔が変わり、どれも「初期設定の見た目」になる。
書体は視覚的な個性のほとんどを決めるので、見出しにだけ投資した。

重さの心配は要らない。Google Fonts は**使われている文字だけを配る**ので、
見出しの数十字なら数KBで済む。本文をOS標準に残しているのは、
フォントの到着を待たずに文章を表示させるため。

明朝を使えるのは、禁止しているのが「クリーム色の背景＋明朝＋テラコッタ」という
**組み合わせ**であって、明朝そのものではないため。白い紙色＋藍なら別物になる。

| 用途 | 書体 |
|---|---|
| 見出し | Zen Old Mincho（明朝） |
| 本文 | OS標準のゴシック（0KB・即表示） |
| 数字・分量・時間 | 等幅。このサイトの中心 |

## 作業のときのお願い

- 変更したら、触ったファイル名と行数を報告する
- 既存のレシピ表示が壊れていないか、自分で確認してから報告する
- 1セクション作り終えたら、コミットを促す
- 出したものを一度は自分で批評する。褒めなくていい

## まだ決まっていないこと（勝手に埋めない）

- チャンネル名の正式表記・ロゴ
- 本番用の連絡先メールアドレス。テスト用の個人アドレスは、公開リポジトリに載せないため work.html から外してある（雛形はコメントで残してある）
- 「このチャンネルについて」の本文（いまは仮の文章）と、はじめた年・更新頻度・拠点の実データ
- 鶏むね肉と茄子の南蛮漬けの**手順**（Instagram のキャプションに記載が無かった）、何人前、時間、鶏むね肉の分量
- すべてのレシピの写真（`assets/images/` が空。Instagram の投稿から持ってくる予定）

これらはコード中で `TODO:` と明記し、それらしい嘘で埋めないこと。

## 仮の文章の扱い

まだ本番の文章が来ていない箇所は `class="draft"` を付けている。
画面上に赤い縦線と「仮の文章」の目印が出る。
本番の文章に差し替えたら `draft` クラスだけ外す。HTMLの中では
`▼▼▼ ここから仮の文章 ▼▼▼` のコメントで挟んであるので、そこを探せばよい。

実データが無い数値は `TODO` と書いておく。それらしい嘘で埋めない。

## SNS

トップの SNS ボタンから飛ぶ先。ここ以外に書かないこと。

| | URL |
|---|---|
| Instagram | https://www.instagram.com/mitamo.kitchen/ |
| TikTok | https://www.tiktok.com/@mitamokitchen |
| YouTube | https://www.youtube.com/channel/UCY5Py2DIsgdurOYtN0ayV3A |

アイコンは公式ロゴではなく、こちらで描いた簡略版のSVG（`tools/build-pages.ts` に書いてある）。
正式なブランドロゴに差し替えたくなったら、各社の利用規約を確認してから入れ替える。

トップの構成は BayashiTV の公式サイトを参考にしている。
ただし、あちらが使っている「字間を広げた英字大文字の小さなラベル」は
使ってはいけない型に入れているので真似しない。

## トップに置くレシピについて

「今週の一品」ではなく「最新のレシピ」と呼ぶ。
更新が週1で続く保証がないため、名前が嘘になるのを避けている。

トップに載せるのは**主な材料だけ**（鶏ネギ飯ならネギダレの4品は載せない）。
レシピページと丸ごと同じ内容にすると、2回とも読み流されるため。

## レシピのデータ（人が書く側と、機械が書く側）

**`dist/recipes.js` は書き出されたもの。直接編集しないこと。上書きされる。**

| ファイル | 誰が書くか | 中身 |
|---|---|---|
| `data/recipes.ts` | **人** | タイトル・タグ・投稿URL・手順の有無 |
| `data/instagram.json` | **機械** | いいね数・コメント数・取り込んだ日 |
| `dist/recipes.js` | 組み立て | 上の2つを合流させたもの。ブラウザが読む |

**分けている理由。** 機械にコメント付きのファイルを書き換えさせると、いつか必ず壊す。
人が書く側と機械が書く側を別のファイルにしておけば、その事故が起きない。

`likes` などが `null` は「まだ取り込めていない」の意味。
**`0` と書かない。** `0` は「いいねが0件」という別の意味になり、嘘になる。

`posted`（投稿日）も同じ扱いで、**分からない回は `null`**。
過去のキャプションから起こした14件は投稿日が分からないので null にしてある。
それらしい日付で埋めると、画面に出る日付が嘘になるうえ、
「新しい順」の並びまで嘘になる。null の回は日付を出さず、
日付のある回より後ろに並ぶ（`tools/site-data.ts` の `byNewest` と `assets/script.js` に同じ規則がある）。

## Instagram の自動取り込み（トークン待ち）

```
node tools/fetch-instagram.ts --dry-run   記録済みの返答で動きを見る（トークン不要）
node tools/fetch-instagram.ts             本番（環境変数 IG_TOKEN が要る）
```

`.github/workflows/fetch-instagram.yml` が毎朝6時（日本時間）に動く。
数値に変化があったときだけコミットするので、無駄な履歴は増えない。

- **トークンはリポジトリのファイルに絶対に書かない。** GitHub の Secrets に `IG_TOKEN` として置く
- 書き換えるのは `data/instagram.json` だけ
- レシピと投稿の対応づけは、`data/recipes.ts` の `instagram`（投稿URL）に含まれる
  短いコードと、API が返す permalink のコードを突き合わせる。
  **投稿URLが書かれていないレシピは対象外**
- **取れなかったときは、前回の数値を残す。**
  null で上書きすると画面から数字が消えて、壊れたように見えるため。
  この動きはテストで固めてある
- 再生数は insights の追加権限が要るので、いまは `null` 固定

### まだできていないこと

- トークンの自動更新（長期トークンの寿命は60日）
- サムネイル画像の取り込み（`thumbnail_url` は一時的なURLなので、保存する処理が要る）
- **新規投稿を自動でレシピページにすること。**
  キャプションに手順が無い回があるため、分量表と手順は人が組んで昇格させる


**自動投稿の対象から外す。** `tools/parse-caption.ts` が `#PR` `#ad` などから判定する。

理由は3つある。

1. タイアップ契約が、Instagram の投稿だけを対象にしているのか、サイトへの転載まで含むのかは案件ごとに違う
2. 日本では2023年10月からステマ規制があり、広告なら広告と分かる表示が要る
3. 商品名とブランド名がそのまま載るため、後から消しにくい

載せると決めた回だけ、手で追加する。そのときは PR である旨を画面に明記すること。

## レシピガチャ（gacha.html）

「今日なにを作るか決まらない」を解くためのページ。単なる遊びではなく、
このサイトで一番よく起きる利用場面に当てている。

- **重複しないように引く。** 同じ料理が並んでも献立にならないため
- そのぶん、引ける数はレシピの数が上限になる。足りないときは
  「10連を引きましたが、いま引ける対象は2件です」と正直に出す。黙って2件だけ出さない
- タグで絞ってから引ける（「鶏肉で、何か1品」という引き方ができる）

## 制作台帳（みたもさんと共有しているページ）

進捗・要件・確認待ちの一覧を1枚にまとめたページ。**みたもさんに共有してある。**

- 元ファイル：`docs/ledger.html`
- 公開URL：**https://claude.ai/code/artifact/58e12afd-b45c-4465-b217-4a7c98f1c160**

**この URL は変えない。** みたもさんはこのリンクをブックマークしている前提。
更新するときは `docs/ledger.html` を書き換えて、**同じ URL を指定して再公開**する。
URL を指定せずに公開すると別のページが増えてしまい、
みたもさんは古いほうを見続けることになる。

更新の手順は `/ledger` スキルにまとめてある。

### 台帳に書くこと・書かないこと

- 書く：いまの状態、これからやること、みたもさんに聞くこと、できないと分かったこと
- 書かない：誰がいつ何を直したかの詳細（Git の履歴が持っている。二重管理すると必ずずれる）

「更新履歴」の節には、みたもさんが**前に見たときから何が変わったか**だけを、
1行で分かる粒度で足す。コミットメッセージをそのまま貼らない。

## 日英対訳表（みたもさんと共有しているページ）

キャプションの英文と日本語を1文ずつ突き合わせた表。台帳と同じく**みたもさんに共有する**。

- 元データ：`data/phrasebook.ts`（人が書く）
- 組み立て：`node tools/build-phrasebook.ts` → `docs/phrasebook.html`
- 公開URL：**https://claude.ai/code/artifact/9f10c029-d390-469d-b102-6274ccf68dae**

**この URL も変えない。** 更新は `data/phrasebook.ts` を足して組み立て直し、
**同じ URL を指定して再公開**する。

### 何のためにあるか

投稿の英文に、意味がずれている箇所がある。とくにまずいのは材料名で、
`light soy sauce`（薄口＝英語では減塩の意味に読まれる）、
`dark soy sauce`（濃口＝中国の老抽を指す）、
`After removing the dashi stock`（出汁ガラ＝取っておくべき出汁を捨てる指示になる）のように、
**店頭でパッケージと照合する**という、このサイトを英語で読む人の目的を壊す。

表の本体は `TERMS`（何度も出てくる言い回しの定訳）で、
投稿ごとの対訳はその裏づけ。件数が増えるほど、直す箇所は減っていく。

### 書くときの約束

- `posted` は投稿されたままを写す。直したものは `fixed` に入れる
- `fixed` が `null` は「そのままでよい」。`verdict: 'ok'` と対にする
- 判断がつかないものは `fixed: null` ＋ `verdict: 'watch'` ＋ `why` に「何が分からないか」。
  **それらしい英語で埋めない**（「推測で埋めない」と同じ理由）
- `tools/build-phrasebook.ts` が書き方の矛盾を検算して、崩れていたら書き出さずに止まる
- **`ja` は投稿された日本語をそのまま写す。誤字も直さない。**
  ここがキャプションと突き合わせる鍵になっているので、1字でも違うと当たらない
  （実際「手に入れることがてきたら」を「できたら」と写していて、直した英語が反映されていなかった）

### 対訳表が画面に効く道すじ

対訳表は、書いただけでは画面が直らない。`tools/phrasebook-apply.ts` が橋を架けている。

| 直すもの | どうやって |
|---|---|
| タイトル・リード文・コツ・グループ名 | 日本語をキーに `fixed` へ差し替える（`POSTS`） |
| 材料名 | 対訳表ではなく `tools/ingredients-ja-en.ts` の辞書を正とする |
| 出てはいけない英語が残っていないか | `tools/check-english.ts`。`npm test` の中で走る |

材料名を辞書側に寄せているのは、対訳表の材料行が「昆布 10g」と
名前と分量をひとつなぎで持っているのに対し、画面は名前と分量を別の欄に出すため。
**定訳が固まったものは、`TERMS` だけでなく辞書にも入れること。**
入れ忘れると、その回だけキャプションの英語のまま出る。

`node tools/generate.ts` は、対訳表に書いてあるのに一度も当たらなかった行を警告する。
これが出たら、日本語が食い違っているということなので必ず見比べる。

### 推奨テンプレート

同じページに、次からのキャプションの型（`TEMPLATE` / `TEMPLATE_RULES` / `EXAMPLE`）を載せてある。
**目印の行は、いま動いているものを変えない。**【レシピ】と [Recipe] を変えると、
過去の投稿を取り込み直せなくなる。新しく足したのは【作り方】と [Steps] の2つだけ。

この2つは、いまの `tools/parse-caption.ts` では読めない。
（【作り方】を置くと、そこから後ろが全部 steps に飲み込まれる。[Steps] は目印自体が無い）
**テンプレートが先、道具があと**という順にしてある。書き方が決まってから合わせる。

`tools/ingredients-ja-en.ts` の材料辞書とは役割が違う。
辞書は**英語が無い回のために材料名を機械的に引く**もの、
対訳表は**英語がある回の文章を人が突き合わせる**もの。
定訳が固まったものは辞書側にも反映する。

## タグの設計

食材・料理の形・場面・時間の4軸。`data/recipes.ts` の `TAGS` に軸ごと定義してある。
材料の分類だけにしないのは、探す人が「弁当に入れたい」「15分しかない」で
探すため。材料の軸だけでは、その問いに答えられない。

**同じタグが `TAG_MIN`（3）件そろうまで、そのタグは画面に出さない。**
押しても1件しか出ないタグは、押した人をがっかりさせるだけなので伏せておく。
データとしては持っているので、件数がそろえば自動で表に出る。
表に出ているタグには件数を添えて、押す前に期待値が分かるようにしてある。

## 英語が無い回の扱い

キャプションに英語が無い回がある（実際に小籠包の回がそうだった）。そのときは：

- **日本語だけで公開する。** 公開を止めると、更新が止まったことに誰も気づかない
- **材料表だけは `tools/ingredients-ja-en.ts` の辞書で英語にする**
- 本文は日本語のみである旨を画面に出す。後から英語が来たら差し替える

**機械翻訳は使わない。** 海外の人がこのサイトを見る一番の目的が
「店頭でパッケージと照合すること」なので、薄口醤油と濃口醤油を
訳し分けられない翻訳は、その目的に対して害になる。
辞書に無い材料は、訳さずに日本語のまま残す。間違った英語より、
日本語のままのほうが辞書やアプリで調べられる。

## 言語ごとに別URLにする（実装済み）

英語での検索流入を本気で取りにいくため、`/en/` に分ける。2026年9月6日決定。

```
/index.html          日本語
/en/index.html       英語
```

- 各ページは片方の言語だけを含む。いまの `.t-ja` / `.t-en` の対は無くなる
- 言語切替ボタンは、同じページの別言語版へのリンクになる
- `hreflang` で相互に指す。**絶対URLでないと効かない**ので独自ドメインが要る
- **材料名の日英併記は残す。** 英語ページでも日本語名を併記する。
  海外の人が店頭でパッケージと照合するためであって、翻訳のためではないので
- **英語の本文が無い回は、英語ページを作らない。**
  中身が日本語のままのページを英語として出すと、検索評価を下げるだけになる。
  つまり英語の流入は、キャプションに英語が書かれているかに依存する

ページが2倍になるため、**HTMLは手で書かない。`node tools/build.ts` が組み立てる。**

## JSON-LD（Google のレシピ検索対策）

**手順・分量・時間がそろっているレシピにだけ書く。**
手順が無いレシピに中途半端な記述を入れても検索結果には出ないし、
それらしい嘘の手順で埋めるのは論外なので、書かないほうがよい。

写真が入るまでは、検索結果に写真つきでは出ない。`image` は
写真を置いた時点で絶対URLで足す（相対パスでは効かない）。

## 開発用の道具（TypeScript）

**閲覧する側にビルドは要らない。** `tools/` は書き出すための道具で、
出来上がった `dist/` の中身は素のHTML・CSS・JS。Node が無くてもそのまま開ける。
コマンドの一覧は冒頭の「よく使うコマンド」にまとめてある。

### なぜ TypeScript にしたか

材料の分量は `null`（キャプションに書かれていなかった）と
文字列（書かれていた）が混ざる。ここを取り違えると
「分量が空のレシピ」や「0と表示されるレシピ」が自動で公開されてしまう。
型があると、その取り違えを書いた時点で止められる。

Node 24 は **TypeScript をそのまま実行できる**ので、ビルド段階は増えていない。

### 大事な注意

**Node は型を消すだけで、検査はしない。**
型を書いただけでは間違いに気づけないので、`npm run check` を必ず走らせること。
実際、この検査で「`match()` が null を返したら落ちる」箇所が1件見つかっている。

外部パッケージは開発用の2つだけ（`typescript` と `@types/node`）。
**サイトには1バイトも含まれない。**

## キャプションからレシピページを作る

```
node tools/generate.ts data/captions/somen.txt <slug>
```

`content/<slug>.js`（レシピ本文のデータ）を書き出し、`recipes.js` に貼る1件分を表示する。
そのあと `npm run build` でページになる。

- ヘッダーとフッターは `index.html` から読み取って使う。
  テンプレートとして別に持つと、片方だけ直したときにズレるため
- **タグは `data/recipes.ts` の TAGS にあるものしか付けない。**
  本文から新しいタグ名を作らせると「お手軽」「手軽」「かんたん」が
  別タグとして増殖して、タグ検索そのものが機能しなくなる
- 香味野菜（ネギ・生姜・にんにく・大葉・みょうが）は「野菜」として数えない。
  ネギが入っているだけの料理を野菜料理として出すと、押した人の期待から外れる
- **PR案件（#PR）は生成を中止する。** 終了コード 2 を返す
- 手順が取れなければ `ready:false`。画面に「手順は準備中」と出る。
  それらしい手順を書き足すことは絶対にしない

## 言語の扱い（組み立て）

- 日本語は `/`、英語は `/en/` に出る
- 言語切替はリンク。同じページの別言語版へ移動する
- リンクは相対パスなので、英語ページのリンクは英語ページ同士でつながる
- **英語の本文が無いレシピは、英語ページを作らない。**
  中身が日本語のままのページを「英語です」と出すと評価を下げるため
- **材料名は英語ページでも日本語を併記する。** 主と従が入れ替わるだけ

### ドメイン

`tools/build.ts` の `SITE_URL` が唯一の出どころ。
hreflang と og:url は絶対URLでないと効かないので、
独自ドメインが決まったらここだけ直して組み立て直す。

## 検索エンジンへの露出（いまは止めている）

`tools/build.ts` の `INDEXABLE` が `false` のあいだ、全ページに noindex が入る。
手順が「準備中」のレシピが検索結果に出ると最初の印象が悪くなるため、
**写真と手順が揃うまでは止めておく**。揃ったら `true` にして組み立て直すだけ。

robots.txt では止めない。止めると noindex を読みに来てもらえなくなる。

## 公開用インフラ（infra/）

AWS の構成をコードで持っている。クリックで作らず、コードで再現できる状態にしてある。

```
S3（非公開）  ←  CloudFront（配信・HTTPS・セキュリティヘッダー）  ←  閲覧者
```

```
cd infra
npm install
npx cdk deploy                        ドメイン無しで公開（CloudFront のURLになる）
npx cdk deploy -c domain=example.com  ドメインを付けて公開
```

### 設計で意識したこと

- **S3 バケットは一切公開しない。** CloudFront からだけ読める（OAC）。
  「バケットを公開設定にして事故る」が静的サイトで最も多い失敗なので、構造で防ぐ
- **GitHub にアクセスキーを保存しない。** OIDC で、その場限りの資格情報を受け取る。
  main ブランチからのみ引き受けられるよう絞ってある
- **セキュリティヘッダーを配信側で付ける。**
  サイトにインラインの `<script>` が1つも無いので、`script-src` を `'self'` だけに絞れている。
  素のHTMLで組んできた副産物
- 証明書（ACM）は CloudFront の仕様で **us-east-1 にしか置けない**ため、スタックごとそこに置いた。
  オリジンの位置は体感に影響しない（CloudFront が各地にキャッシュするため）
- `/en/` のようにディレクトリで来たら `index.html` を返す。
  S3 は「ディレクトリ」を知らないので CloudFront Functions で補う。
  **`serve.ps1` にも同じ処理を入れて、手元と本番の挙動を合わせてある**

### デプロイ

`.github/workflows/deploy.yml` が main への push で動く。3つに分けてある。

| 仕事 | いつ動くか |
|---|---|
| `build` | **いつも。** 型検査・検証・組み立て。ここが緑なら壊れていない |
| `pages` | 変数 `PUBLISH_PAGES` が `true` のときだけ |
| `aws` | 変数 `AWS_DEPLOY_ROLE_ARN` が入っているときだけ |

**送り先が未設定のうちは、送る仕事は動かさずに飛ばす。失敗にしない。**
設定していないだけで毎回失敗のメールが届くと、
本当に壊れた日にそのメールを見なくなる。同じ理由で
`fetch-instagram.yml` も、`IG_TOKEN` が無ければ何もせずに終わる。

`cdk deploy` の出力（DeployRoleArn / BucketName / DistributionId）を
リポジトリの Variables に入れると `aws` が動き出す。
秘密ではないので Secrets ではなく Variables でよい。

GitHub Pages に出すなら、設定画面で2つ。
**Settings → Pages → Source を「GitHub Actions」に**した上で、
Variables に `PUBLISH_PAGES = true` を足す。
Source が「Deploy from a branch」のままだとこの仕事は失敗するので、
変数で明示的に有効にするまで動かさないようにしてある。

### 依存の版がずれると npm ci が落ちる

`package.json` と `package-lock.json` は**必ずそろえること。**
`npm ci` は食い違うと即座に落ちる（`npm install` と違って直してくれない）。
実際、手元で新しい版に入れ替えたときに package.json を直し忘れて、
main への push が毎回失敗していた。
版を上げたら `npm install` を走らせて、lock ごとコミットする。

### 実行前に必要なもの

- AWSアカウント（**ルートに多要素認証**、予算アラート）
- AWS CLI（`winget install Amazon.AWSCLI`）
- `npx cdk bootstrap`（そのアカウント・リージョンで初回だけ）
